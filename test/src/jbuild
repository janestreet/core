(library
 ((name core_test)
  (libraries (bigbuffer_blocking
              bignum
              core
              core_kernel_test
              expect_test_helpers))
  (preprocess (pps (ppx_jane -dont-check-doc-comments-attachment ppxlib.runner)))
  (preprocessor_deps (config.h))
))

(rule
 ((targets (config.h))
  (deps (${ROOT}/lib/core_kernel/src/config.h))
  (action (bash "cp ${<} ${@}"))))


(jbuild_version 1)
