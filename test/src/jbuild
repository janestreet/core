(library
 ((name core_test)
  (libraries (core
              core_kernel_test
              expect_test_helpers))
  (flags (:standard -safe-string))
  (preprocess (pps (ppx_jane ppx_driver.runner)))
))


(jbuild_version 1)
