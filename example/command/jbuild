(executables
 ((names (main main_no_recur))
  (libraries (async
                    core
                    fieldslib
                    sexplib))
  (preprocess (pps (ppx_jane)))))


(jbuild_version jane_street)
