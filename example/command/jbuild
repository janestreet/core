(executables
 ((names (main main_no_recur))
  (libraries (async
                    core
                    fieldslib
                    sexplib))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))


(jbuild_version jane_street)
