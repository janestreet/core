(executables
 ((names (main main_no_recur))
  (libraries (async
                    core
                    fieldslib
                    sexplib))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
