(executables
 ((names (main main_no_recur))
  (libraries (async
                    core
                    fieldslib
                    sexplib))))

(enforce_style ())

(jbuild_version jane_street)
